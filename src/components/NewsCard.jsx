import React from 'react';

const NewsCard = ({singleNews}) => {
    console.log(singleNews);
    return (
        <div>
            {singleNews. title}
        </div>
    );
};

export default NewsCard;